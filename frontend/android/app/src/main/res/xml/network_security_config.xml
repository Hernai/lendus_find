<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite cleartext HTTP solo a hosts de desarrollo locales.
  Todo lo demás (producción) sigue exigiendo HTTPS por default.
  - 10.0.2.2 es la IP del host Mac desde el emulador Android
  - localhost / 127.0.0.1 cubren depuración con port-forward de adb
-->
<network-security-config>
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">10.0.2.2</domain>
    <domain includeSubdomains="true">localhost</domain>
    <domain includeSubdomains="true">127.0.0.1</domain>
  </domain-config>
</network-security-config>
