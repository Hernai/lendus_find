# Sube el heap de Node para el build de producción.
# Vite + vue-tsc + el bundle de Monaco (≈3.7 MB) revientan los 2 GB por
# default y matan el proceso con "JavaScript heap out of memory",
# especialmente en Windows. 8 GB es holgado y no afecta dev/CI.
node-options=--max-old-space-size=8192
